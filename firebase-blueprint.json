{
  "entities": {
    "UserDetail": {
      "title": "UserDetail",
      "description": "Basic details for a TechIndiana student.",
      "type": "object",
      "properties": {
        "name": { "type": "string", "description": "The student's full name." },
        "grade": { "type": "string", "description": "The student's grade or level." },
        "areaOfInterest": { "type": "string", "description": "The student's primary area of technical interest." }
      },
      "required": ["name", "grade", "areaOfInterest"]
    }
  },
  "firestore": {
    "/users/{userId}": {
      "schema": "UserDetail",
      "description": "User profile data stored by their unique ID."
    }
  }
}
